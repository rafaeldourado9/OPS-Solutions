[project]
name = "crm"
version = "0.1.0"
description = "White-label CRM with WhatsApp agent integration"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"
